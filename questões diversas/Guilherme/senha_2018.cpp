#include <bits/stdc++.h>
using namespace std;

int main() {
    int num = 0, vezes = 0;
    while (num != 2018) {
        cin >> num;
        vezes++;
    }
    cout << vezes - 1 << endl;
    return 0;
}