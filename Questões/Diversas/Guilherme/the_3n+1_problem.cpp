#include <bits/stdc++.h>
using namespace std;

// Esse problema permanece sem ter sido resolvido.

int three_n_one(int n) {
    int 
    if (n % 2 == 1) return 3 * n + 1;
    else return n / 2;
}

int main() {
    unsigned int i, j;
    while (true) {

    }
    return 0;
}